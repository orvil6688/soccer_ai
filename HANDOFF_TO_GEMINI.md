# 骨架交付文件 v2 — 世界盃足球分析系統（小c → 小g）

> **用途**：本檔是經總司令逐項拍板後的需求骨架，作為小g 撰寫「四大板塊規格書」的輸入。
> **非規格書本身**：小g 收到後須依四大板塊 SOP（目錄與防線 / Mermaid / 契約 / Phase 計畫）展開。
> **紀律遵循**：本檔自包含；標記 🔒 者為已鎖定決策不得更動；標記 ⚠️ 者為小g 須在規格中寫死精確值的契約缺口，不得以「沿用」或「同前」帶過（CLAUDE.md 13.1）。

---

## 〇、專案定位（一句話）

掃描世界盃賽事 → 在賽前不同時間點對讓分盤與大小球抓「初盤→收盤」快照 → 結合基本面與盤口心理找出和莊家的歧見 → 出精選推薦 → 存歷史推薦 → 隔日回填賽果做回測，用於迭代篩選規則。

**這不是「預測比分」系統，是「找出莊家定價歧見」的博弈系統。** AI 信心度非真實勝率，全程須標 🤖 AI 推論。

---

## 一、🔒 已拍板決策清單（不得更動）

| # | 決策項 | 拍板結果 |
|---|---|---|
| 1 | 開發範圍 | **完整版**：快照存檔 + AI 分析 + 回測閉環 |
| 2 | 首要目標賽事 | 2026 世界盃（6/11 開幕，僅剩 11 天） |
| 3 | 盤口種類 | 亞洲讓分盤 + 大小球（不做 1X2/獨贏為主） |
| 4 | 資金管理 | **凱利公式取消**。先用固定注碼：價值高 2 單位、一般 1 單位 |
| 5 | 主資料源 | API-Football 免費層（賽季後可無痛接俱樂部聯賽） |
| 6 | 備援資料源 | BALLDONTLIE 免費層（交叉驗證盤口/賽果，非骨幹） |
| 7 | 爬蟲（球探007/Scrapling） | **後期實驗模組**：手動觸發、可失敗、不進主流程 |
| 8 | 快照策略 | 錨定每場開賽時間，抓 初盤 / 中段(T-12h) / 收盤(T-1~2h) 三錨點 |
| 9 | 回測 | 隔日抓前一日賽果回填，算命中率，回頭調篩選規則 |
| 10 | AI 角色 | 沿用 GEM 開盤手人設當 prompt；數據掃描改用 API 非爬蟲 |
| 11 | 施工優先序 | 6/11 前先上線「快照存檔 + 賽果回填」；誘盤/公允盤/xG 為後續 Phase |
| 12 | 執行環境 | 脫離 Colab，改正式專案結構（見第五節） |

---

## 二、資料層架構（一主一備一實驗）

| 角色 | 來源 | 職責 | 失敗影響 |
|---|---|---|---|
| 主骨幹 | API-Football 免費層（100 次/日） | 賽程、盤口快照、傷兵、賽果回填 | 致命：壞了主流程停 |
| 交叉驗證 | BALLDONTLIE 免費層 | 比對盤口/賽果，對不上標警訊 | 部分：壞了僅少了驗證 |
| 進階走勢（後期） | 球探007 + Scrapling | 細緻盤口走勢，手動觸發 | 部分：抓到加分、抓不到照常 |

**⚠️ 重要事實，小g 須在規格中明示**：任何免費 API 都不存「初盤」歷史，只回「當下盤口」。初盤必須靠本系統按三錨點排程自行抓取入庫——這是系統核心工作量，非 API 能代勞。

---

## 三、⚠️ 需小g 在規格「資料結構與全域紀律契約」中鎖定的缺口

以下為 API 相關契約缺口，小g 須**核對官方文件後寫死精確值並標注核對日期**，不得憑記憶：

1. **世界盃 league id**：API-Football 推測為 `1`；BALLDONTLIE 為 FIFA World Cup 2026 專屬端點。→ 兩者都須核對。
2. **盤口 bet id / market 名稱**：亞盤與大小盤在 API-Football 的 bet id（原碼用 4/5）、在 BALLDONTLIE 的 market 命名。
3. **bookmaker 優先序**：原碼用 [4,8,41]（Pinnacle/Bet365/1xBet），須核對；Pinnacle 為 sharp 基準應優先。
4. **結算口徑**：淘汰賽讓分/大小是否以 90 分鐘結算（延長賽不計）——須向 API 確認。
5. **兩源欄位對映表**：API-Football 與 BALLDONTLIE 的隊名、賽事 id、盤口數值如何對齊，才能交叉驗證。
6. **免費層 rate limit**：API-Football 100/日已知；BALLDONTLIE 免費層每分鐘/每日上限須查明，排程器據此節流。

---

## 四、選注引擎邏輯（核心，小g 須轉成可調參數 + 可回測假設）

> 設計原則：每條規則都是「假設」，價值在於被回測驗證，不是拿來盲信。全部做成設定檔可調參數。

### 4.1 選注六步流程

1. **估公允盤**（基本面）：球隊近況、傷兵、賽事動機。xG 為後期接入（見 4.4）。
2. **比對莊家盤找歧見 (edge)**：自估公允盤 vs 莊家盤的落差。
3. **誘盤過濾（對抗莊家）**：
   - 盤口「太甜」優先假設為陷阱，非便宜
   - 反向線移動（公眾沒押的方向在動）才視為真錢訊號
   - 關鍵數字 0 / 0.25 / 0.5、2 / 2.5 / 3 的跨越比移動幅度更重要
   - ⚠️ **限制須明示**：Ticket% vs Money%（散戶單量 vs 大戶金額背離）任何可用 API 都不提供，只能用「線往公眾反向移動」近似，規格不得假裝做得到
4. **世界盃動機檢查**：
   - 小組末輪死亡之組（已出線/已淘汰）→ 輪休/默契球風險 → 降權或跳過
   - 淘汰賽、開幕戰 → 大小球偏小
   - 公眾人氣隊（巴西/阿根廷）盤口灌水 → 價值常在冷門或小球方
5. **注碼**：固定制（價值高 2 單位、一般 1 單位）。凱利取消。
6. **回測閉環**：賽後回填 → 算命中率 → 回頭調 1-4 步的參數。

### 4.2 篩選訊號參數化（初版預設值，待回測校準）

| 訊號 | 規則化 | 方向 | 預設門檻 |
|---|---|---|---|
| 變動幅度 | 讓分/大小移動量 | 越大越優先 | ≥ 0.25 球 |
| 反向線移動 | 線往公眾反向移 | 跟隨聰明錢 | 加權 |
| 關鍵數字 | 盤口停在整/半數 | 謹慎 | 標記 |
| 動機失真 | 死亡之組/已定生死 | 降權或跳過 | 降權 |
| 賽事階段 | 淘汰賽/開幕戰 | 偏小球 | 偏向 Under |
| edge 門檻 | 公允盤 vs 莊家盤落差 | 達標才出手 | ⚠️ 待總司令定，暫設 ≥0.25 球 |

### 4.3 世界盃情境特例（小g 須寫入篩選邏輯）

世界盃一天僅 3-4 場，**省 API 的漏斗篩選暫不啟用**（全抓即可）。漏斗邏輯保留，賽季後接俱樂部 20 場/天時再啟用。

### 4.4 xG / 進階數據定位（後期 Phase）

- xG 用途：估公允盤、找歧見，**非預測比分**。
- ⚠️ **預測力警告須明示**：世界盃國家隊剛集結、無共同樣本，club 級 xG/個人數據預測力大打折扣。故世界盃版「盤口心理 + 賽事動機」權重應高於 xG。
- 數據源：API-Football 免費層無原生 xG，後期評估付費源（Sportmonks/TheStatsAPI 含 xG）或俱樂部賽季再上。

---

## 五、建議目錄結構（小g 寫「目錄結構與防線定義」用）

```
soccer_ai/
├── soccer_ai/
│   ├── __init__.py
│   ├── config.py          # 🔒 共用：路徑/金鑰/常數/league id/盤口契約 唯一真實來源
│   ├── api_client.py      # 板塊一：API-Football 抓取（節流/逾時/額度控管/isinstance）
│   ├── bdl_client.py      # 板塊一：BALLDONTLIE 交叉驗證（可失敗）
│   ├── odds_parser.py     # 板塊二：盤口解析（回傳型別固定 str,float）
│   ├── snapshot.py        # 板塊二：三錨點快照排程 + 變動計算（系統核心）
│   ├── storage.py         # 板塊二：歷史推薦 & 快照 & cache 持久化
│   ├── selector.py        # 板塊三：選注引擎（4.1-4.3 邏輯，參數化）
│   ├── analyzer.py        # 板塊三：Gemini 分析（GEM 人設 prompt）
│   ├── backtest.py        # 板塊四：賽果回填 + 命中率計算
│   └── main.py            # 板塊四：主流程編排 + 失敗分流
├── scrapers/              # 後期實驗，獨立可失敗，不被 main 依賴
│   └── titan007.py        # 球探007 + Scrapling（Phase 後段）
├── data/                  # 歷史/快照/cache JSON（gitignored）
├── .env / .env.example
├── requirements.txt / .gitignore / .gitattributes
└── CLAUDE.md
```

**🔒 絕對不動清單（建議寫入規格）**：`config.py` 金鑰讀取、`storage.py` 歷史檔寫入格式、已上線的快照排程錨點邏輯。

---

## 六、已修正的原版缺陷（小g 寫入事件學習庫 13.3）

| # | 原版問題 | 修正方式 |
|---|---|---|
| 1 | 金鑰寫死（已外洩，總司令須重置） | 改 `os.getenv()` 從 .env 讀 |
| 2 | 建 `Soccer_AI_Apisport` 卻寫 `Soccer_AI_Data` → crash | 路徑常數集中 config.py，建立與寫入共用同一變數 |
| 3 | Gemini prompt 組好卻從未呼叫 | analyzer.py 補上 generate_content |
| 4 | 無 timeout/節流/額度控管 | api_client 加 timeout、throttle、daily cap |
| 5 | 只查 date=TODAY 卻想看未來 24h | 查 今天+明天，按開賽時間錨點過濾 |
| 6 | TODAY 用 UTC 但下游用 +8 → 跨日錯位 | 統一由帶 +8 的 NOW 推導 |
| 7 | best_odd 型別 float/str 混用 | 解析函式回傳固定 (str, float) |
| 8 | 全裸 except: pass 吞錯 | 具名例外 + logging，分致命/部分失敗 |
| 9 | 無 isinstance 防禦 | 讀 API 陣列前一律 isinstance（契約 D） |

---

## 七、建議 Phase 切分（小g 寫「模組化逐步計畫」參考；對應 🔒 施工優先序）

> 鐵律：6/11 前「快照存檔 + 賽果回填」必須能跑，否則回測永遠生不出 p 值。其餘往後排。

- **Phase 1（6/11 前必須）**：config + api_client + snapshot + storage。能對世界盃賽事按三錨點抓盤口存歷史。
- **Phase 2（6/11 前必須）**：backtest 賽果回填 + 命中率。閉環的最後一塊。
- **Phase 3**：selector 選注引擎（4.1-4.3 規則參數化）+ analyzer（GEM 人設）。出推薦。
- **Phase 4**：bdl_client 交叉驗證 + edge 門檻調校。
- **Phase 5（後期）**：xG 接入、scrapers 爬蟲實驗、俱樂部賽季漏斗啟用。

---

## 八、交付小結（給小g 的施工提示）

1. 本骨架所有 🔒 決策已定，小g **不得推翻**，僅就 ⚠️ 缺口在規格中補上精確值。
2. 六個 ⚠️ API 契約缺口（第三節）與 edge 門檻（4.2），請小g 在規格「資料結構與全域紀律契約」逐一寫死、標注核對日期。
3. 請依四大板塊 SOP 展開：①目錄與防線 → ②Mermaid 流程圖（畫出三錨點快照→選注→回測閉環）→ ③契約 → ④Phase 計畫。
4. 通用契約 I（AI 推論透明化）為強制：所有 Gemini 輸出標 🤖 AI 推論。
5. 交付前小c 將執行 13.2 強制審查清單（8 點）+ 通用契約 H（把握度 %）後才亮綠燈。

---

**骨架版本**：v2
**狀態**：待小g 展開為四大板塊規格書
**遺留待總司令拍板**：edge 出手門檻精確值（4.2 暫設 ≥0.25 球）
