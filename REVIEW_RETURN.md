# 小 c 審查回退清單 — 規格書 v2.0 → v2.1（小c → 小g，經總司令轉交）

> **審查結論：把握度 82%，未達 95% 綠燈標準，退回補一輪。**
> 骨幹邏輯正確、Phase 切分守住 6/11 死線、90 分鐘結算口徑鎖定正確。
> 但有 3 個 🔴 嚴重漏洞會逼小cc 自行推測（違反 13.4），加上部署環境需重新設計。
> 補完下列各項後重新送審。

---

## 〇、總司令已裁定的新決策（小g 須吸收進 v2.1）

1. **部署環境 = GitHub Actions 排程**（免費、無伺服器）。
2. **凱利已取消**（沿續 🔒 決策，再次確認，v2.0 無誤）。
3. **小g 新增的「Pinnacle vs 1xBet 反向線移動」訊號：採納方向，但不得直接觸發 2 單位警報。** 降級為「加權訊號 + 記錄進回測」，待回測在世界盃樣本驗證有正期望值後，才升格為自動觸發。理由：未經回測的規則不得繞過閉環直接下最高注碼，違背系統設計哲學。

---

## 一、🔴 嚴重漏洞（必補，否則小cc 會推測）

### 1.1 match_id 主鍵設計錯誤
- **問題**：v2.0 用 `YYYYMMDD_TEAM1_TEAM2`（隊名縮寫）當主鍵。隊名在 API-Football 與 BALLDONTLIE 不一致（Mexico vs MEX）、翻譯與特殊字元（Côte d'Ivoire）會出錯，導致 Phase 4 交叉驗證對不上。
- **修正**：主鍵改用 **API-Football 回傳的數字 `fixture_id`**。隊名僅作人類可讀輔助欄位。BALLDONTLIE 的賽事另存其 game id，並在欄位對映表記錄兩者對應關係。

### 1.2 TEST_MODE 行為未定義（8 點清單第 4 點）
- **問題**：`.env` 有 `TEST_MODE` 旗標，但規格書未定義其行為。
- **修正**：依通用契約 C 寫死三件事——(a) 測試資料隔離到 `data/test/` 子目錄、(b) 加 🧪 視覺標記、(c) 嚴禁覆蓋/污染正式歷史檔與快照庫。

### 1.3 Gemini 輸出字數預算未定（8 點清單第 3 點）
- **問題**：analyzer 產出長度無上限。
- **修正**：寫死各欄位截斷上限（信心度說明、傷兵影響、操盤解讀分別幾字），明確是各自截斷還是合併截斷，避免 JSON 無限膨脹。

---

## 二、🟡 中度漏洞（建議補）

### 2.1 快照漏抓補救邏輯（與部署環境合併解決，見第四節）
- **問題**：三錨點是核心，但某錨點時 API 掛了/額度盡，初盤永遠遺失，v2.0 無補救。
- **修正**：見第四節「窗口容錯」設計。

### 2.2 BALLDONTLIE 賽事對齊方法過於描述性（8 點清單第 7 點）
- **問題**：「名稱模糊比對或日期對齊」非精確邏輯。模糊比對閾值多少？對不上算「衝突警訊」還是「忽略」？
- **修正**：Phase 4 才實作，可標「待 Phase 4 定」，但須明確標注為待定項，不得含糊帶過。

### 2.3 CLV 出現在 Mermaid 但無對應實作
- **問題**：流程圖有「計算命中率與 CLV」，但 Phase 2 實作清單只寫命中率、無 CLV 計算方式。
- **修正**：CLV 需「下注時的線 vs 收盤線」，三錨點資料足夠。要嘛補上 CLV 計算進 Phase 2，要嘛從 Mermaid 移除，圖與實作須一致。

---

## 三、🟢 小問題

### 3.1 Telegram 環境變數缺失
- 3.1 節提到「發送 Telegram 警示」，但 `.env.example` 無 Telegram token/chat_id。
- 修正：要嘛補進環境變數契約，要嘛 Phase 1 先用 logging、Telegram 警示往後排。

---

## 四、部署環境重新設計（GitHub Actions 特性修正，最重要）

> **核心事實**：GitHub Actions 排程不準時（實測延遲常 30-60 分鐘）、高負載會默默跳過且無告警、repo 60 天無活動會自動停。
> **直接後果**：不能用「排在正好 T-1h 去抓收盤」的天真設計——收盤延遲到開賽後才跑，資料就報廢。

### 4.1 觸發機制：從「定時抓」改為「高頻檢查 + 窗口容錯」
- workflow 改為 **每 15-20 分鐘跑一次**（非排在精確錨點）。
- 每次跑只做：檢查「哪場、哪錨點的快照還沒抓、且現在落在其抓取窗口內」→ 有就補抓。
- 即使某次延遲/跳過，下次（15 分鐘後）自動補上。

### 4.2 錨點改為「時間窗口」而非精確點
| 錨點 | v2.0（精確點） | v2.1（窗口） |
|---|---|---|
| 初盤 | 第一次開盤 | 掃到該場首次有盤即抓，標記完成 |
| 中段 | T-12h | 開賽前 11~13h 窗口內任一次抓到即可 |
| 收盤 | T-1h | 開賽前 45~90 分鐘窗口內任一次抓到即可 |
- 窗口內每次檢查都試，抓到就標記完成、不重抓（省額度）。
- 收盤窗口結束仍沒抓到 → 發告警，該場標「收盤缺失」，回測時排除或降權。

### 4.3 GitHub Actions 特定契約（須寫進 v2.1）
- **狀態持久化**：Actions 每次跑都是全新環境、跑完銷毀。「已抓哪些快照」的狀態與抓到的 JSON，**每次跑完必須 git commit 推回 repo**（對應通用範本「排程執行完自動 push」），否則下次無從得知上次進度。
- **金鑰改用 GitHub Secrets**：三把 API key 放 repo Settings → Secrets，workflow 以 `${{ secrets.XXX }}` 注入，非 .env。環境變數契約須補這條（本機開發用 .env、CI 用 Secrets，雙軌並存）。
- **時區陷阱**：Actions cron 走 UTC，與系統內部 UTC+8 須明確換算，寫進契約避免錯位。
- **repo 活動**：世界盃僅一個月，60 天自動停影響小，但須知悉。

### 4.4 回覆小g 原提問
- 小g 問「Cron 每小時 vs 微秒級 Scheduler」——**兩者皆否決**。微秒級是假需求（盤口非微秒變動）；GitHub cron 不可信賴到不能依賴單一精確時間點。採用 4.1 的「高頻檢查 + 窗口容錯」。

---

## 五、補完後預期

修正 3 個 🔴 + 3 個 🟡 + 部署環境重設計後，預期把握度可達 95%+，屆時亮綠燈交總司令裁決、再交小cc 施工。

**仍待總司令拍板**：edge 出手門檻精確值（v2.0 暫設 ≥0.25 球，可接受作為回測起點）。

---

**清單版本**：v1（對應規格書 v2.0 → v2.1）｜審查者：小c｜日期：2026-06-02
